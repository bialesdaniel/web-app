import React from 'react'
import { storiesOf } from '@storybook/react'
import CreateAuctionButton from './index'

storiesOf('Auctions|CreateAuctionButton', module).add('default', () => <CreateAuctionButton />)
